// function Refresh(timeoutPeriod) {
// 	setTimeout("location.reload(true);",timeoutPeriod);
// }
// window.onload = Refresh(5000); //reload every 5 second

var type = new Typed(".auto-type",
	{
		strings: ["Security Solution", "Protection"],
		typeSpeed: 50,
		backSpeed: 50,
		loop: true
	})
